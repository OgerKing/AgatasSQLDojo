# Agatas SQL Cech – copy to .env and set values.
# Backend (server/) reads DATABASE_URL and PORT when run locally.

DATABASE_URL=postgres://cech:cech@localhost:5432/cech
PORT=3001
JWT_SECRET=dev-jwt-secret-change-me

# Optional: run schema on server start (e.g. in Docker)
# RUN_MIGRATIONS=1
