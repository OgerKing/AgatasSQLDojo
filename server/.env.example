# Copy this file to .env in this folder (server/.env)
# Then start Postgres (e.g. docker compose up -d postgres) and run: npm run dev

DATABASE_URL=postgres://cech:cech@localhost:5432/cech
PORT=3001
RUN_MIGRATIONS=1
JWT_SECRET=change-me-in-production
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
